// Публічний help-асистент (anon, без history-persist): власний короткий
// max-tokens, per-IP добовий ліміт і глобальний добовий бюджет-запобіжник.
// Значення продуктові, однакові в усіх середовищах — тому в коді, не в `.env`.
export const HELP_CHAT = {
    /** Стеля відповіді провайдера — тримає відповіді стислими і дешевими. */
    maxTokens: 800,
    /** Скільки запитів дозволено одному IP за добу. */
    ipLimit: 20,
    /**
     * Скільки запитів усього дозволено анонімам за добу. Запобіжник поверх
     * per-IP ліміту: фронт деградує до статичних статей при вичерпанні.
     */
    dailyBudget: 1000,
} as const;
