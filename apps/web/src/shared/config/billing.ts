/**
 * Демо-банер на сторінці тарифу («тестова картка, кошти не списуються»).
 *
 * Прод-значення — `false`, і воно тут же у коді: банер бачать реальні
 * користувачі, тож хибне `true` у проді — це видиме неправдиве твердження про
 * гроші. Для локальної перевірки банера тимчасово постав `true` і поверни
 * назад перед деплоєм.
 */
export const BILLING_DEMO_MODE = false;
