export interface UiVerifiedBadgeProps {
    /**
     * `sm` — компактний варіант для карток списку (каталог), `md` — під назвою
     * отримувача на самій публічній сторінці.
     */
    size?: 'sm' | 'md';
    /** Контекстні відступи місця використання. */
    className?: string;
}
