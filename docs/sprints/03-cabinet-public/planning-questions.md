# Sprint 3 — Журнал рішень планування

> **Статус:** **постійний артефакт**, не тимчасовий чек-лист.
>
> Документ фіксує продуктові і технічні рішення, прийняті під час планування Sprint 3, з обґрунтуванням і закритими питаннями. Виконує роль, аналогічну `docs/product/qr-decisions.md` для QR-стандарту. На нього посилаються runtime-константи (`packages/types/src/constants/reserved-slugs.ts`) і майбутній `docs/sprints/03-cabinet-public/README.md`.
>
> Кожен пункт у форматі «питання → варіанти → рішення». Закриті — позначаються ✅ + дата + rationale. Журнал змін — внизу.

---

## A. Архітектурні блокери (закрити **до** старту коду)

- [x] **A1.** ✅ 2026-05-02 — **один проект**, host-aware routing. Cookie входу залишається на host-level (`finly.com.ua`, без leading dot), щоб публічна зона не бачила сесію. Google-вхід — на кабінетному домені.

- [x] **A2.** ✅ 2026-05-02 — **дві кнопки**: «Інший банк» (нова адреса `qr.bank.gov.ua`) + «Інший банк (запасний варіант)» (стара адреса `bank.gov.ua/qr`). Обидві адреси зашиті в коді як константи (env-змінна `NBU_PAYLOAD_LINK_HOST` зникає). QR-6 з блокера запуску перетворюється на post-launch перевірку: за реальним feedback зрозуміємо, чи варто прибрати запасну. Старий формат (002) як був hardcoded на `bank.gov.ua/qr` — без змін.

- [x] **A3.** ✅ 2026-05-02 — **тільки вивіска**: QR + кнопки банків. Клієнт сам вписує суму в банк-додатку. Список рахунків бізнесу не показуємо (privacy-ризик). Конкретний рахунок завжди має окреме посилання `pay.finly.com.ua/{business}/{invoice}` (Sprint 4) — ФОП сам надсилає його клієнту.

---

## B. UX-питання (закрити на kickoff, до верстки)

- [x] **B1.** ✅ 2026-05-02 — **11 логотипів банків неактивні** (сірі, не клікабельні) з підписом «Незабаром». Активні лише дві кнопки внизу: «Інший банк» (нова адреса) + «Інший банк (запасний варіант)» (стара адреса). Візуально показуємо майбутній стан, поведінково чесно.

- [x] **B2.** ✅ 2026-05-02 — **варіант 1 + 3 разом**: та сама верстка під toggle всередині кабінету (основний preview, ФОП не виходить з контексту) + кнопка «Відкрити в новій вкладці» (додатково для перевірки "як насправді"). Iframe з реальною публічною сторінкою всередині не робимо — зайва складність.

- [x] **B3.** ✅ 2026-05-02 — **URL-фундамент незмінний:** `pay.finly.com.ua/{business}` (вивіска) і `pay.finly.com.ua/{business}/{invoice}` (рахунок). Slug бізнесу:
  - **Free:** випадкові 8 символів (приклад: `aB3xQ9k7`), не редагується.
  - **Paid:** ФОП обирає (приклад: `ivanenko`).

  Для рахунку (Sprint 4): Free — теж випадкові 8 символів, Paid — формат на вибір ФОП. Залишається open question на Sprint 4: чи примусово додавати випадковий шматочок навіть у Paid (захист від послідовного підбору `zamovlennia-1`, `-2`, ...) або обмежитись попередженням ФОП-у. Зараз не вирішуємо.

- [x] **B4.** ✅ 2026-05-02 — **не закладаємо нічого під майбутнє**. Кабінет бізнесу у Sprint 3 — проста сторінка (налаштування + QR), без вкладок-заглушок. UI під рахунки узгоджуємо в Sprint 4 цілісно.

- [x] **B5.** ✅ 2026-05-03 — **реальні логотипи банків**. Якщо колись хтось із банків попросить прибрати — заміняємо на generic. Юридичний ризик мінімальний поки не позиціонуємо себе як партнера банку.

- [x] **B6.** ✅ 2026-05-03 — **за замовчуванням всі 11 банків увімкнені** + **мінімум 1 обов'язковий** (не можна зняти всі). Лінивий ФОП отримує максимальне покриття без зусиль; активний — налаштовує під себе; нульовий стан неможливий.

---

## C. Технічні питання (зафіксувати в плані спринту)

- [x] **C1.** ✅ 2026-05-03 — **два окремі поля**: `taxationSystem` (enum: `simplified-1`, `simplified-2`, `simplified-3`, `general`) + `isVatPayer` (boolean). Семантично точно (можна бути на спрощеній-3 + ПДВ). Валідаційне правило: `isVatPayer === true` дозволено лише при `simplified-3` або `general`. Юр-ревʼю Sprint 6 структуру не змінить — лише підтвердить.

- [x] **C2.** ✅ 2026-05-03 — **hard-delete одразу, без soft-delete**. Видалення бізнесу:
  - Confirm dialog перед видаленням.
  - Попередження, якщо у бізнесу є активні рахунки ("видаливши, клієнти з посиланнями не зможуть оплатити").
  - "Undo" toast з таймером 5 секунд (стандартний Gmail-style паттерн).
  - Hard-delete одразу — назавжди.
  - Slug звільняється одразу.

  Поле `deletedAt` у схемі (Sprint 1 заклав "на майбутнє") залишаємо невикористаним — коштує нуль, дає гнучкість якщо колись передумаємо. Soft-delete фіча (restore, корзина) — overengineering для MVP.

- [x] **C3.** ✅ 2026-05-03 — **розширюємо до повноцінного категоризованого списку**. Файл `packages/types/src/constants/reserved-slugs.ts` оновлений з 5 категоріями: TECHNICAL (12), SYSTEM (35), GOVERNMENT (17), BRANDS (111), PUBLIC_FIGURES (26). Загалом 201 зарезервоване ім'я. Розширюється точково по запиту/інциденту, без міграцій. **Окрема задача (не блокер):** нецензурщина як окремий файл з leetspeak-варіантами + open-source список. **Конверсійна логіка:** при колізії з reserved — повідомлення "зверніться до підтримки" → канал sales-конверсії для правовласників бренду.

- [x] **C4.** ✅ 2026-05-03 — **whitelist полів публічного endpoint**: `type`, `name`, `slug`, `acceptedBanks`. Все інше (IBAN, taxId, taxationSystem, isVatPayer, paymentPurposeTemplate, ownerId, managers, createdAt) **не віддається** в JSON. Реквізити доступні клієнту тільки через QR (відкривається банк-додатком). Найбезпечніший шлях: відсутність дублюючих каналів витоку.

- [x] **C5.** ✅ 2026-05-03 — **знак гривні в центрі обох QR** (нормативно правильно для payment-QR + візуальна узгодженість для URL-QR). **Лого Finly — як окремий UI-елемент під QR** (у верстці сторінки, не всередині картинки).

  **Хвіст Sprint 2 (треба виправити в Sprint 3):**
  - Замінити `apps/api/src/modules/qr/assets/finly-logo-qr.png` → `hryvnia-symbol.png` за зразком з PDF постанови НБУ № 97 (білий круг з знаком ₴, розмір — нормативний; норматив §II.11-12 ст. 5).
  - `QrLogoCompositor` стає `QrCenterSymbolCompositor` (або зберігається назва, але семантично — знак гривні, не довільне лого).
  - QR-4 manual check (`docs/manual-checks/README.md`) переформулюємо: "QR зі знаком гривні в центрі зчитується банк-додатками", без згадки "лого Finly".
  - Лого Finly додається у верстці кабінету і публічної сторінки як SVG/PNG під QR — це задача Sprint 3 frontend-частини.

  **Custom-logo бізнесу (Paid фіча, Sprint 6) лишається в плані** — теж піде в центр, замість знаку гривні. Це знаходиться у конфлікті з нормативом, але так було вирішено бізнесово ще в qr-decisions §2.2 (трактуємо як "ФОП-вибір з юридичним усвідомленням").

- [x] **C6.** ✅ 2026-05-03 — **знято**. Між Sprint 3 і Sprint 6 деплой на прод не планується — реальних ФОП з кількома бізнесами не існуватиме. Grandfather-логіки і feature-flag-у на ліміт у Sprint 3 **не закладаємо взагалі**. Sprint 6 додасть ліміт як звичайну валідацію — без додаткових міграцій / `if`-ів для legacy-юзерів.

- [x] **C7.** ✅ 2026-05-02 — закрито разом з A2. Адреси hardcoded як константи; env зникає.

- [x] **C8.** ✅ 2026-05-03 — **покроково (4 кроки)** з валідацією на кожному кроці і **step-навігатором зверху** з підсвічуванням поточного кроку (ФОП бачить весь шлях і "де я"):
  - **Крок 1:** Тип + Назва
  - **Крок 2:** IBAN + ІПН
  - **Крок 3:** Оподаткування + ПДВ
  - **Крок 4:** Призначення платежу + Банки

  Кліком по попередньому кроку у навігаторі — повертаємось редагувати. Перехід на наступний крок — лише після успішної валідації поточного.

---

## E. Залишкові блокери (виявлені після першого проходу A/B/C/F/G)

Це питання, що вилізли при review закритих рішень. Закриваються до старту написання Sprint 3 README.

- [x] **E1.** ✅ 2026-05-03 — **case-preserved display + case-insensitive uniqueness/lookup** (Twitter/Instagram-style).

  - Регекс slug-у міняємо: дозволяємо `[A-Za-z0-9]` + дефіси.
  - Зберігаємо в БД **два поля**: `slug` (як написав ФОП — `IvanEnko`) і `slugLower` (нормалізований lowercase — `ivanenko`).
  - **Unique index — на `slugLower`**, не на `slug`. `VasyLenKO` блокує реєстрацію `vasylenko` і `VASYLENKO`.
  - **Lookup public-сторінки** — query по `slugLower` (вхід нормалізується перед запитом).
  - **301 редірект**: якщо URL case ≠ збережений case → редірект на канонічну форму (як ФОП зберіг).
  - **Reserved-перевірка** — теж на lowercase (наявний список вже весь lowercase, без правок).
  - **Free random retry** — при колізії на lowercase (рідко, але можливо) перегенеровуємо.
  - **QR-картинка** генерується з канонічним URL (з регістром, як ФОП зберіг) — клієнт одразу потрапляє на правильну адресу без редіректу.

- [x] **E2.** ✅ 2026-05-03 — **перейменовуємо Dashboard → Бізнеси**. Не плутаємо клієнта фейковою назвою (Dashboard зараз порожня заглушка). Якщо колись з'явиться реальна аналітика — додамо як нову сторінку.

  **Зміни:**
  - Шапка кабінету: **Бізнеси** / Profile / Billing.
  - URL: `/business` (список), `/business/{slug}` (кабінет бізнесу), `/business/{slug}/invoice/{slug}` (Sprint 4).
  - Видалити поточну заглушку `/dashboard` (`apps/web/src/app/(protected)/dashboard/`).
  - Оновити `apps/web/src/middleware.ts` — `PROTECTED_PATHS` `'/dashboard'` → `'/business'`; редірект з auth-сторінок після логіну `'/dashboard'` → `'/business'`.
  - Оновити `apps/web/src/entities/navigation/headerNavStore` (назва пункту).

- [x] **E3.** ✅ 2026-05-03 — **гібрид: `noindex` за замовчуванням, ФОП-toggle для opt-in**.

  **Що робимо в Sprint 3:**
  - Поле `seoIndexEnabled: boolean` у схемі Business, default `false`.
  - Toggle "Показувати сторінку в Google" у кабінеті бізнесу — доступний **усім** (без Paid-перевірки в Sprint 3).
  - Публічна сторінка рендерить `<meta name="robots" content="index">` або `noindex` залежно від поля.

  **Що додає Sprint 6:** Paid-gating на toggle (Free бачить disabled з підказкою "доступно на Paid"). Між Sprint 3 і Sprint 6 деплою на прод немає (C6) — конфлікту з legacy-юзерами не виникне.

- [x] **E4.** ✅ 2026-05-03 — **видалити** `'/pay'` з `PROTECTED_PATHS` у `apps/web/src/middleware.ts`. Жодних редіректів — фіча ніколи не була запущена, реальних легасі-посилань не існує.

- [x] **E5.** ✅ 2026-05-03 — **toggle у дропдауні профілю в шапці**. Завжди видно — це **головний conversion-тригер на Paid**.

  **Нова монетизаційна логіка** (відрізняється від business-flow.md §6 — її треба оновити, див. G7):
  - **Free:** ФОП для власного бізнесу (1 шт., ліміт у Sprint 6). Bookkeeper-режим **недоступний**.
  - **Paid:** unlock bookkeeper-режиму + інші Paid-фічі.

  **UI:**
  - Toggle "Режим бухгалтера" у дропдауні профілю (де аватарка / ім'я).
  - Під назвою — **завжди видимий** одно-рядковий опис muted-кольором: "вести бізнеси клієнтів, які ще не зареєстровані у Finly". Без tooltip-у на hover (це порушило б `responsive.md` §6 — hover-only інформація заборонена). Inline-текст однаково читаємий на mobile і desktop.
  - **Sprint 3:** toggle працює без Paid-перевірки (як E3 SEO toggle).
  - **Sprint 6:** Free клацнув → модалка "Режим бухгалтера доступний на Paid. Оформити підписку?".

  **Без онбординг-питання** — зайвий крок розбиває conversion-момент.

- [x] **E6.** ✅ 2026-05-03 — **редагування існуючого бізнесу: inline edit per field** (Stripe / Linear / Notion-style settings).

  **UX:**
  - Кабінет бізнесу = сторінка з картками-секціями: "Основне", "Реквізити", "Оподаткування", "Призначення і банки".
  - Кожне поле — readonly з іконкою "олівець" поряд.
  - Click "олівець" → поле стає editable → ✓ зберегти / ✗ скасувати.
  - Persist миттєво (без global "Save" внизу сторінки).

  **Coupled-поля** (валідуються разом, редагуються однією карткою):
  - `taxationSystem` + `isVatPayer` (ПДВ дозволений тільки на спрощеній-3 / загальній — C1).
  - `acceptedBanks` — список галочок (не "одне поле").

  **Backend:**
  - `PATCH /businesses/me/:id` приймає часткові зміни (одне поле або coupled-група).
  - Partial Zod-схема + валідація coupled-правил на app-layer.

  **Створення (C8) лишається 4-step** — це onboarding-флоу, інша задача. Стандартна практика розводити "create" і "edit" UX.

- [x] **E7.** ✅ 2026-05-03 — **додаємо 2 QR-коди на публічну сторінку** (по одному на кожну адресу — нову і стару).

  **Повна структура публічної сторінки:**
  - Заголовок «Оплата на {Тип} {Назва}» (наприклад «ФОП Іваненко»).
  - 11 неактивних логотипів банків з підписом «Незабаром» (рішення B1).
  - **Дві пари "кнопка + QR":**
    - «Інший банк» + QR з NBU-payload на новій адресі (`qr.bank.gov.ua`).
    - «Інший банк (запасний варіант)» + QR з NBU-payload на старій адресі (`bank.gov.ua/qr`).
  - Підпис під QR: «Або відскануйте з вашого банк-додатка».

  **Чому два QR — а не один:** обидва шляхи (нова / стара адреса) мають бути доступні і через тап, і через скан. Симетрія: якщо одна адреса не спрацювала на тапі — клієнт пробує запасну; якщо одна не спрацювала на скані — теж пробує запасну.

  **Технічно:**
  - `QrService.renderForNbuPayload(input, '003', { host: 'qr.bank.gov.ua' })` → перший QR.
  - `QrService.renderForNbuPayload(input, '003', { host: 'bank.gov.ua/qr' })` → другий QR.
  - Обидва — зі знаком гривні в центрі (рішення C5).
  - Mobile-розмір QR ≥ 200×200 px для надійного сканування з іншого пристрою.

  **Очікуваний UX-ефект:** охоплюємо і «тих, хто тапає кнопку», і «тих, хто звик сканувати з банк-сканера». Конверсія в оплату вища.

---

## F. Не-блокери (фіксуємо рішення на час імплементації)

Не потребують окремого обговорення — стандартні паттерни / наявна інфраструктура.

- [x] **F1.** ✅ 2026-05-03 — підтверджено. `BusinessAccessGuard` за зразком `JwtActiveGuard`, перевіряє `user._id === business.ownerId` АБО `user._id ∈ business.managers`. 403 при failed-перевірці.

- [x] **F2.** ✅ 2026-05-03 — підтверджено. Public — стандартний Next.js 404. Cabinet — окрема сторінка "У вас немає доступу до цього бізнесу" + кнопка "Повернутись до моїх бізнесів".

- [x] **F3.** ✅ 2026-05-03 — **усі сторінки адаптивні (mobile + tablet + desktop), mobile-first як залізобетонне правило**. Створено окрему конвенцію `docs/conventions/responsive.md` + додано в `docs/conventions/README.md` index, `CLAUDE.md` і `AGENTS.md` (обидва agent-entrypoints). Не "бажано для cabinet" — обов'язково всюди.

- [x] **F4.** ✅ 2026-05-03 — підтверджено. Next.js Server Components + `revalidate: 60` (60-секундний кеш). Зміни ФОП-у видно клієнтам із затримкою до хвилини. Балансує швидкість оновлення і навантаження на API.

- [x] **F5.** ✅ 2026-05-03 — підтверджено. Loop: generate → check unique (на `slugLower` за E1) → retry. Max 10 спроб; на 11-й — кидаємо помилку ("спробуйте ще раз"), що статистично нереально досягти.

- [x] **F6.** ✅ 2026-05-03 — підтверджено. Empty state: ілюстрація + заголовок "У вас поки немає бізнесів" + CTA-кнопка "Створити перший бізнес".

- [x] **F7.** ✅ 2026-05-03 — підтверджено. Спираємось на існуючий глобальний `ThrottlerGuard` (60 req/min). Окремих обмежень для публічного endpoint не додаємо — реальний клієнт не генерує багато запитів, бот швидко впирається в загальний ліміт.

- [x] **F8.** ✅ 2026-05-03 — підтверджено. **Frontend-only Undo, без backend state**:
  - Click "Видалити" → frontend оптимістично ховає бізнес з UI + показує toast з 5s-таймером. **Жоден запит на backend не йде.**
  - Click "Скасувати" в межах 5s → frontend скасовує таймер, бізнес повертається в UI.
  - Минуло 5s без скасування → frontend шле `DELETE` → backend робить **миттєвий hard-delete**.
  - **Browser unload / nav-away протягом 5s = implicit cancel.** Запит на видалення не відправляється — це безпечніший дефолт за "пробуємо догнати unload".
    - Чому не `sendBeacon`: він підтримує лише POST, не DELETE. Власні методи (`fetch` з `keepalive: true`) теж не дають **гарантії** доставки — браузери їх часто відкидають при unload.
    - UX-наслідок: якщо ФОП закрив вкладку у вікно 5 секунд після кліку "Видалити" — бізнес лишається. Він побачить його при наступному логіну і клацне "Видалити" знову. Прийнятна поведінка для edge-case-у; альтернативи дають **недетермінованість** ("видалився чи ні — як пощастить").

  **Чому не backend transient flag** (відкинутий варіант): `setTimeout` у Node не переживає рестарт процесу і не працює в multi-instance API. Бізнес залишався б "прихованим але не видаленим" — технічний борг прямо у diff-і.

---

## G. Хвости Sprint 2 (окремий блок у плані Sprint 3)

Зміни, що випливають з рішень A2 і C5 і потребують виправлень у вже закритих артефактах Sprint 2.

- [x] **G1.** ✅ 2026-05-03 — підтверджено. Прибрати `NBU_PAYLOAD_LINK_HOST` env-змінну (рішення A2).
  - `apps/api/src/config/env.ts` — видалити з required-перевірки і з типу `Env`.
  - `.env.example` — видалити запис.
  - `apps/api/src/test-setup.ts` — видалити fallback-присвоєння.
  - `CLAUDE.md` секція "Configuration & Environment" — видалити з API required.
  - `apps/api/src/modules/qr/qr.service.ts` — `renderForNbuPayload` приймає `host` параметром (для двох кнопок) або викликається двічі з обома константами (рішення під час імплементації).

- [x] **G2.** ✅ 2026-05-03 — підтверджено. Замінити центральний QR-asset (рішення C5).
  - `apps/api/src/modules/qr/assets/finly-logo-qr.png` → `hryvnia-symbol.png`.
  - Зразок — з PDF постанови НБУ № 97 (`docs/product/qr-spec/nbu-003-spec.pdf`, §II.11-12 ст. 5: білий круг з ₴, розмір нормативно прив'язаний до QR-версії).
  - `nest-cli.json` `compilerOptions.assets` — оновити шлях.
  - `QrLogoCompositor` — методу і коментарям дати точнішу семантику (це вже не довільне лого, а норматив-asset).

- [x] **G3.** ✅ 2026-05-03 — підтверджено. Переформулювати QR-4 у `docs/manual-checks/README.md`.
  - Поточно: "QR з логотипом Finly всередині все одно сканується".
  - Стає: "QR зі знаком гривні в центрі сканується банк-додатками".
  - Решта тексту (як перевірити, що може піти не так) — оновити відповідно.

- [x] **G4.** ✅ 2026-05-03 — підтверджено. Оновити Sprint 2 README (`docs/sprints/02-qr-core/README.md`).
  - Секція §2.0 "Spec acquisition" — рішення про host (`NBU_PAYLOAD_LINK_HOST` як required env) **знято** на користь "дві константи, дві кнопки". Описати рішення A2 і посилання на planning-questions.md.
  - Секція §2.3 — `QrLogoCompositor` тепер працює з нормативним asset-ом, не з довільним лого.
  - Manual UAT — замість QR-6 ("вибір хоста") тепер пост-launch перевірка "яка з двох кнопок частіше спрацьовує".

- [x] **G5.** ✅ 2026-05-03 — підтверджено. Оновити `docs/product/qr-spec/README.md`.
  - Секція "Host у нормативі" — додати пункт що Finly реалізував "обидва hosts через дві кнопки", щоб не вгадувати.
  - Секція "Host вирішення (post QR-6)" — переформулювати під нову модель (QR-6 більше не gating-factor для launch, а post-launch metric).
  - Зняти fail-fast інваріант на env (бо env більше не існує).

- [x] **G6.** ✅ 2026-05-03 — підтверджено. Оновити `CLAUDE.md` секція "QR generation pipeline".
  - Згадати, що використовується дві константи host (нова + стара) і дві кнопки на UI.
  - Прибрати згадку про `ENV.NBU_PAYLOAD_LINK_HOST`.

- [x] **G7.** ✅ 2026-05-03 — підтверджено. Оновити `docs/product/business-flow.md` (рішення E5).
  - Секція §2 "Ролі користувачів і режим бухгалтера" — додати: bookkeeper-режим = **Paid-only**.
  - Секція §6 "Free vs Paid (MVP)" — переписати pricing-модель: відмовитись від "за кількістю бізнесів" як єдиної шкали; нова шкала: **Free = 1 свій бізнес; Paid = bookkeeper-режим + multiple businesses + інші paid-фічі**.
  - Секція §2 "центр продукту — користувач у режимі бухгалтера" — лишити (підкріплює нову модель).

---

## D. Свідомо поза скоупом Sprint 3

- Інвойси (Sprint 4).
- Індивідуальні per-bank кнопки (Sprint 5).
- Free vs Paid ліміти, кастомний логотип у QR (Sprint 6).
- Preview-mode для сторінки інвойсу (Sprint 4 разом з інвойсами).
- UI «вкладки Постійний QR / Рахунки» (Sprint 4 разом з інвойсами).
- Передача бізнесу між акаунтами, запрошення керівника (Phase 1.5+).

---

## Журнал рішень

| Дата | Питання | Рішення | Rationale |
|---|---|---|---|
| 2026-05-02 | A1 — два сайти | Один проект з host-aware routing | На MVP-обсязі два проекти не виправдані; cookie на host-only щоб не текла в публічну зону |
| 2026-05-02 | A2 — адреса «Інший банк» | Дві кнопки: «Інший банк» (нова адреса) + «Інший банк (запасний варіант)» (стара адреса) | Знімає блокер QR-6 — клієнт сам пробує, який варіант спрацює. Адреси hardcoded як константи (env зникає). Sprint 2 §2.0 потребує оновлення (env required → hardcoded constants) |
| 2026-05-02 | C7 — env для NBU host у dev | Закрито разом з A2 — env більше не потрібен (адреси hardcoded) | — |
| 2026-05-02 | A3 — головна сторінка бізнесу | Тільки вивіска (QR + кнопки банків), без списку рахунків | Privacy-ризик для MVP; ФОП-toggle — зайве рішення для 90% користувачів |
| 2026-05-02 | B1 — кнопки банків UX | 11 неактивних логотипів («Незабаром») + 2 активні кнопки «Інший банк» / «Інший банк (запасний)» | Чесно і візуально передбачає Sprint 5; клієнт бачить які банки підтримуватимуться |
| 2026-05-02 | B2 — preview-режим у кабінеті | Та сама верстка під toggle + кнопка «Відкрити в новій вкладці» | ФОП не виходить з контексту, але має фолбек для повної перевірки |
| 2026-05-02 | B3 — формат slug-у | URL-фундамент `/{business}/{invoice}` незмінний. Free: випадкові 8 символів обидві частини. Paid: ФОП редагує | Простіше і чесніше — Free не плутає клієнта псевдо-vanity префіксами, Paid повністю на ФОП-і |
| 2026-05-02 | B4 — кабінет бізнесу | Без вкладок під майбутні інвойси. Проста сторінка налаштування + QR | Уникаємо архітектурних спекуляцій під ще не вирішений Sprint 4 UI |
| 2026-05-03 | B5 — логотипи банків | Реальні логотипи зі сайтів банків | Максимальна впізнаваність; юр-ризик низький поки не позиціонуємо себе як партнера; заміна на generic як план Б |
| 2026-05-03 | B6 — мінімум банків | Дефолт усі 11 + мінімум 1 обов'язковий | Лінивий ФОП — максимальне покриття; активний — гнучкість; нульовий стан неможливий |
| 2026-05-03 | C1 — тип оподаткування | Два поля: `taxationSystem` (enum 4-х значень) + `isVatPayer` (boolean) | Юр-семантично точно; галочка ПДВ дозволена тільки на спрощеній-3 / загальній |
| 2026-05-03 | C2 — видалення бізнесу | Hard-delete одразу: confirm + попередження про активні рахунки + Undo 5s toast. Slug звільняється одразу | Soft-delete для бізнесу = overengineering на MVP; реальні сценарії покриті простими UX-захистами |
| 2026-05-03 | C3 — reserved slugs | Розширений категоризований список (~200 імен): технічні + системні + державні + ~110 брендів + ~24 публічні особи. Нецензурщина — окремий файл (не блокер) | Захист майбутніх продажів VIP-конверсії + UX страховка від конфліктів |
| 2026-05-03 | C4 — публічні поля | Whitelist: type, name, slug, acceptedBanks. Реквізити лише в QR | Відсутність дублюючих каналів витоку — найбезпечніший шлях |
| 2026-05-03 | C5 — лого в QR | Знак гривні в центрі обох QR (нормативно), Finly-лого під картинкою як UI-елемент. Хвіст Sprint 2: замінити `finly-logo-qr.png` → `hryvnia-symbol.png` + переформулювати QR-4 | Норматив compliance + офіційний вигляд; Finly-брендинг переноситься в верстку |
| 2026-05-03 | C6 — ліміт бізнесів | Не закладаємо нічого. Sprint 6 робить як звичайну валідацію | Між Sprint 3 і Sprint 6 деплою на прод немає — legacy-користувачів не існуватиме |
| 2026-05-03 | C8 — форма створення бізнесу | Покроково 4 кроки + step-навігатор з підсвічуванням поточного, з можливістю клікнути назад | Стандартний onboarding-патерн; зменшує "страшність" 8-польової форми; навігатор дає відчуття контролю |
| 2026-05-03 | E1 — регістр slug-ів | Case-preserved display + case-insensitive uniqueness/lookup. Поля `slug` + `slugLower`, 301 редірект на канонічну форму | Twitter/Instagram-патерн; ФОП-бренд зберігає вигляд, клієнт не страждає від помилки регістру; brand-squatting у різному регістрі неможливий |
| 2026-05-03 | E2 — навігація кабінету | Перейменовуємо `Dashboard` → `Бізнеси`; URL `/business`, `/business/{slug}` | Не тримати фейкову назву; реальний Dashboard додамо коли буде наповнення |
| 2026-05-03 | E3 — SEO індексація | `noindex` за замовчуванням + ФОП-toggle (Sprint 3 — без Paid-gating, Sprint 6 додасть) | Безпечний дефолт + power-user opt-in; інструмент є — людина вирішує |
| 2026-05-03 | E4 — legacy `/pay` route | Видалити з middleware | Мертвий рудимент; піддомен `pay.finly.com.ua` — окрема історія |
| 2026-05-03 | E5 — toggle "режим бухгалтера" | Дропдаун профілю в шапці. Bookkeeper = Paid-only (новий conversion-тригер). Без онбординг-питання | Главний апсел-тригер, треба бути завжди видимим; онбординг-питання розбиває момент конверсії |
| 2026-05-03 | E6 — редагування бізнесу | Inline edit per field (Stripe/Linear/Notion-style). Картки-секції, олівець на полі, миттєвий persist. Coupled-поля редагуються однією карткою | Створення (4-step onboarding) і редагування ("просто підправити") — різні задачі; стандартна практика розводити їх UX |
| 2026-05-03 | E7 — QR на публічній сторінці | 2 QR-коди (по одному на кожну адресу) поряд з 2 кнопками "Інший банк" | Охоплюємо обидва шляхи оплати: тап і скан з банк-додатка; конверсія вища |
| 2026-05-03 | F1-F8 — не-блокери | Підтверджено усі дефолти (BusinessAccessGuard, 404-сторінки, mobile-first як правило, 60s cache, retry на slug-collision, empty-state, rate-limit, frontend-only Undo з implicit-cancel при unload) | Стандартні паттерни / наявна інфраструктура; нічого нового вирішувати не треба |
| 2026-05-03 | G1-G7 — хвости Sprint 2 | Підтверджено пакетом усі 7 правок: видалення `NBU_PAYLOAD_LINK_HOST` env, заміна QR-asset на знак гривні, переформулювання QR-4, оновлення Sprint 2 README + qr-spec README + CLAUDE.md + business-flow.md | Виконуються в межах Sprint 3 як окремий блок "cross-sprint cleanup" |
